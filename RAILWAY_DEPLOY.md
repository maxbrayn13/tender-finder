# 🚀 ПРАВИЛЬНЫЙ ДЕПЛОЙ НА RAILWAY

## ⚠️ ВАЖНО: 2 отдельных сервиса!

Railway требует создать **2 отдельных сервиса**:
- 🔴 Backend (Python/FastAPI)
- 🔵 Frontend (React/Vite)

---

## 📋 БЫСТРАЯ ИНСТРУКЦИЯ

### ШАГ 1: Зайти на Railway

1. Откройте https://railway.app
2. Нажмите **"Login with GitHub"**
3. Авторизуйте Railway

---

### ШАГ 2: Создать новый проект

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите и выберите **"maxbrayn13/tender-finder"**

⚠️ Railway создаст 1 сервис. Это нормально! Мы настроим его как Backend.

---

### ШАГ 3: Настроить Backend сервис

**После создания первого сервиса:**

#### 3.1 Переименовать сервис (опционально)
```
Нажмите на название сервиса → Rename → "backend"
```

#### 3.2 Открыть Settings
```
Нажмите на сервис → Settings (вкладка справа)
```

#### 3.3 Настроить Root Directory
```
Service → Root Directory

Введите: backend
```

#### 3.4 Настроить Start Command
```
Service → Start Command

Введите: uvicorn main:app --host 0.0.0.0 --port $PORT
```

#### 3.5 Добавить переменные окружения
```
Variables → New Variable

Name: DATABASE_PATH
Value: ./goszakup_lots.db
```

#### 3.6 Deploy!
```
Railway автоматически начнёт деплой
Ждите 3-5 минут
```

#### 3.7 Скопировать Backend URL
```
Settings → Domains → Public Domain

Скопируйте URL, например:
https://tender-finder-backend-production.up.railway.app
```

---

### ШАГ 4: Создать Frontend сервис

#### 4.1 Добавить новый сервис в том же проекте
```
В левой панели нажмите "+ New"
Выберите "GitHub Repo"
Выберите "maxbrayn13/tender-finder" (тот же репозиторий!)
```

#### 4.2 Переименовать сервис
```
Rename → "frontend"
```

#### 4.3 Открыть Settings

#### 4.4 Настроить Root Directory
```
Service → Root Directory

Введите: frontend
```

#### 4.5 Настроить Build Command
```
Service → Build Command

Введите: npm run build
```

#### 4.6 Настроить Start Command
```
Service → Start Command

Введите: npm run preview -- --host 0.0.0.0 --port $PORT
```

#### 4.7 Добавить переменную окружения
```
Variables → New Variable

Name: VITE_API_URL
Value: [ВСТАВЬТЕ URL BACKEND ИЗ ШАГА 3.7]

Например:
https://tender-finder-backend-production.up.railway.app
```

⚠️ **БЕЗ СЛЭША в конце!**

#### 4.8 Deploy!
```
Railway автоматически начнёт деплой
Ждите 5-7 минут
```

---

## ✅ ШАГ 5: Проверить что всё работает

### Backend:
```
1. Откройте Backend URL в браузере
   https://[ваш-backend].up.railway.app

2. Должны увидеть:
   {
     "message": "TenderFinder API v1.0",
     "docs": "/docs"
   }

3. Откройте /docs для Swagger:
   https://[ваш-backend].up.railway.app/docs
```

### Frontend:
```
1. Откройте Frontend URL
   https://[ваш-frontend].up.railway.app

2. Должны увидеть:
   - Главную страницу TenderFinder
   - 2 калькулятора
   - Статистику
```

---

## 🎯 СТРУКТУРА В RAILWAY

После настройки у вас должно быть:

```
Проект: tender-finder
├── 🔴 backend (Python/FastAPI)
│   ├── Root: backend
│   ├── Start: uvicorn main:app --host 0.0.0.0 --port $PORT
│   ├── Var: DATABASE_PATH=./goszakup_lots.db
│   └── URL: https://tender-finder-backend-xxx.up.railway.app
│
└── 🔵 frontend (Node/Vite)
    ├── Root: frontend
    ├── Build: npm run build
    ├── Start: npm run preview -- --host 0.0.0.0 --port $PORT
    ├── Var: VITE_API_URL=https://[backend-url]
    └── URL: https://tender-finder-frontend-xxx.up.railway.app
```

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Ошибка: "pip: command not found"
**Решение:** Проверьте Root Directory = `backend`

### Ошибка: "npm: command not found"
**Решение:** Проверьте Root Directory = `frontend`

### Ошибка 404 на Frontend при запросах к API
**Решение:** Проверьте переменную VITE_API_URL:
```
1. Frontend сервис → Variables
2. VITE_API_URL должен быть БЕЗ слэша в конце
3. Redeploy frontend после изменения
```

### Backend деплоится, но не отвечает
**Решение:** Проверьте Start Command:
```
uvicorn main:app --host 0.0.0.0 --port $PORT
```

### Frontend собирается, но показывает ошибки
**Решение:**
```
1. Проверьте что Backend URL правильный
2. Откройте Developer Tools (F12)
3. Посмотрите ошибки в Console
4. Проверьте Network tab - идут ли запросы к Backend
```

---

## 🔄 КАК ОБНОВЛЯТЬ

После изменений в коде:

```bash
git add .
git commit -m "Update: описание изменений"
git push
```

Railway **автоматически** перезагрузит оба сервиса!

---

## 💡 СОВЕТЫ

### 1. Проверяйте логи
```
Нажмите на сервис → Deployments → View logs
```

### 2. Используйте переменные
```
Все секреты и URL в Variables, не в коде!
```

### 3. Custom Domain
```
Settings → Domains → Add Domain
```

### 4. Мониторинг
```
Metrics → CPU, Memory, Network
```

---

## 🎉 ГОТОВО!

**Ваше приложение онлайн:**
- Backend API: https://[ваш-backend].up.railway.app
- Frontend: https://[ваш-frontend].up.railway.app

**Покажите сайт кому угодно!** 🚀

---

## 📞 ПОМОЩЬ

**Проблемы с деплоем?**
1. Проверьте логи (View logs)
2. Убедитесь что Root Directory правильный
3. Проверьте переменные окружения
4. Создайте Issue на GitHub

---

**Сделано с ❤️ для тендерных специалистов Казахстана**
